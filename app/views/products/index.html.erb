<span class="pull-right">
  <%= link_to "ลงประกาศ", new_product_path(type: 'board'), class: 'btn btn-default' %>
</span>
<div class="clearfix"></div>
<div class="ibox">
  <div class="ibox-title">
    สินค้าขายล่าสุด
  </div>
  <div class="ibox-content">
    <% @products.each do |product| %>
      <%= product.title %>
    <% end %>
  </div>
</div>